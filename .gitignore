# Secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node
frontend/node_modules/
frontend/dist/

# NOTE (presentation mode): the raw CSV and the generated analytics JSON
# (frontend/public/data/**) are BOTH committed — the dataset travels with the repo so
# anyone can rebuild, and CI deploys the static site without calling the LLM.
# ⚠ These contain real call transcripts — keep the repo PRIVATE if that data is sensitive.

# NOTE: the generated analytics JSON (frontend/public/data/**) IS committed now —
# we generate it locally and deploy a static site, so CI never calls the LLM.
# (Heads-up: these JSON files contain call transcripts/quotes — keep the repo private
#  if that data is sensitive.)

# LLM analysis cache
.llm_cache/

# TS build cache
frontend/tsconfig.tsbuildinfo
